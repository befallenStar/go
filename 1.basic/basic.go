package main

import "fmt"

func main() {
	// 1、标记：包括关键字、标识符、常量、字符串、符号
	// 2、go不需要行分隔符;，go编译器会自动完成这个工作
	// 3、注释：和c的注释一样
	// 4、标识符：表示变量和常量等，规则与c一样
	// 5、字符串连接：通过+号就可以实现字符串的连接
	// 6、关键字：大部分和c相同，也有一些特有的，比如func、go、select、defer、chan、fallthrough等
	// 7、空格：变量声明必须使用空格隔开，其余部分可以使用空格使代码美观
	fmt.Println("basic")
}
